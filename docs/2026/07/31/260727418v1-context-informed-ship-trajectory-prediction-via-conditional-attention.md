# Context-Informed Ship Trajectory Prediction via Conditional Attention

- 区域：精读区
- 排名：9
- 匹配度：4.1/10
- 来源：arxiv
- 作者：Yuan Guan, Chandler Squires, Timothy Hu, Pradeep Ravikumar
- 机构：Carnegie Mellon University, Lockheed Martin
- 链接：[arXiv / Source](http://arxiv.org/abs/2607.27418v1) · [PDF](https://arxiv.org/pdf/2607.27418v1)

## TLDR
The paper proposes the Conditional Informer, a Transformer encoder-decoder that frames ship trajectory prediction as a conditional generation task by using directional cross-attention from vessel states to environmental contexts (weather/metadata) and a Modality Masking training strategy to handle missing sensor data, achieving 15.4% higher accuracy than kinematic/concatenation baselines and nearly an order-of-magnitude lower fallback error under modality mismatch.

## Abstract
Long-term ship trajectory prediction is a fundamental capability for maritime safety and autonomous navigation. While recent Transformer-based architectures have improved forecasting horizons, they predominantly rely on historical kinematic states, treating vessel motion as an isolated system. In reality, maritime navigation is profoundly modulated by extrinsic factors like weather and constrained by static vessel characteristics. Existing multimodal approaches fundamentally model the joint distribution over states and contexts, treating environmental variables as peer features rather than encoding the directional physical dependence of vessel dynamics on environmental conditions.
  In this work, we propose the Conditional Informer, a novel encoder-decoder architecture that formulates trajectory prediction as a conditional generation task. We employ a dedicated Conditional Attention mechanism where the vessel state explicitly queries environmental contexts through cross-attention, encoding the physical prior that weather modulates - but is not generated by - vessel dynamics. Furthermore, to address the intermittency of real-world data, we introduce a Modality Masking training strategy to prevent catastrophic degradation during sensor fallback. Extensive experiments on AIS and ERA5 data demonstrate that our approach outperforms kinematic and concatenation-based baselines by 15.4% in prediction accuracy when context is available. Crucially, Modality Masking prevents shortcut learning, reducing fallback error by nearly an order of magnitude compared to unconstrained models.


## 精读解读（中文）
### 一、研究动机
现有基于Transformer的船舶轨迹预测模型主要依赖历史运动状态，将船舶视为孤立系统，忽略了天气等外部环境因素对船舶运动的物理影响。已有多模态方法将环境和状态作为对等特征联合建模，未能编码环境对船舶动力学单向调控的物理先验，容易导致捷径学习与特征崩溃。

### 二、技术方案（Method）
提出Conditional InformerTP架构，将轨迹预测建模为条件生成任务P(Y|X,W)。采用多模态嵌入层，分别对状态（经纬度、SOG、COG等5维）、环境上下文（风速、有效波高等3维）和静态元数据（船体特征、货类等9维）进行独立投影并加入位置编码与时间特征嵌入。编码器作为上下文建模器聚合状态与环境序列，解码器作为状态生成器通过Conditional Attention机制进行条件生成：该机制包含状态自注意力（ProbSparse，复杂度O(L log L)）、上下文交叉注意力（状态生成查询、环境提供键值，方向性编码环境调控状态的物理不对称性）以及元数据广播条件化投影。训练采用MSE损失，并引入Modality Masking训练策略，在训练时随机屏蔽上下文或元数据模态以模拟传感器回退场景，防止部署时模态缺失导致的性能崩溃。

### 三、结果（Result）
在AIS与ERA5数据上的实验表明，所提方法在上下文可用时相比仅运动学模型和拼接式多模态基线，预测精度提升15.4%。Modality Masking策略有效防止捷径学习，在传感器回退（上下文缺失）情境下将回退误差降低近一个数量级。

### 四、结论（Conclusion）
通过显式编码环境对船舶运动的单向物理影响，并在训练中模拟模态缺失，Conditional Informer在准确性和鲁棒性上均优于对等融合的基线，为多模态轨迹预测提供了因果结构先验与部署鲁棒性的有效方案。

### 五、方法论与关键技术细节
关键实现细节包括：概率稀疏注意力将复杂度降为O(L log L)；交叉注意力使用独立于自注意力的查询投影，以学习对不同模态的不同询问；时间嵌入共享日历特征保证状态与环境对齐；评价指标采用全预测时域的平均Haversine距离而非终点误差，以支持中途航路点预测；Modality Masking针对部署模态失配（DMM）问题，可视为广义的利用特权信息学习（LUPI）设置。局限性体现在关注物理可测量的风和波浪因素，尚未考虑港口拥堵、地缘事件等非物理外生因素，但架构可扩展至额外信号。
